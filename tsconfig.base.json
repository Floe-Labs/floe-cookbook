{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Floe Cookbook — shared base",
  "_comment": "Shared, strict base for the TypeScript examples. Each example stays independent: it opts in with { \"extends\": \"../tsconfig.base.json\" } and adds its own `include`. Do not add project references or paths here — examples must not depend on each other.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "moduleDetection": "force",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "skipLibCheck": true,
    "noEmit": true
  }
}
